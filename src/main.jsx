Element.prototype.findChild = function(name, recursive) {
    this
}

var date = Date.now(), diff = 0; player = {};
function loop() {
    diff = Date.now() - player.last_played;
    // updateHTML()
    calc(diff/1000*dev.speed)
    updateTemp()
    player.last_played = Date.now()
}

function getApp() {
    return document.getElementsByTagName("app")[0]
}

getApp()