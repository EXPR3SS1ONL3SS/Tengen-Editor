package com.tengen;
class interpreter {
    static String toTypeScript(String code) {
        String ts = "";
        String[] lines = code.split("\n");
        for (String string : lines) {
            if (string.contains("for (")) {

            };
        }
        return ts;
    }
}

